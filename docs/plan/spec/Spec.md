# 完全設計書 — 配電盤上部確認用ロボット 昇降・カメラシステム

**実装（ノード名・トピック名・ピン・ファイル構成）は書かない。次工程の詳細設計に回す。**
読み方・位置づけは [README.md](README.md)。

> 出どころのある事実とユーザー決定だけを書き、決まっていない所は `未定` とし
> [Spec-open.md](Spec-open.md) の ID を付けた。**推測で埋めない**
> （推測は次のセッションで別の推測になる）。

---

## 1. 対象

配電盤上部を確認するロボットのうち、**カメラを盤上部へ持っていき、向け、映すまで**のすべて。
本システムは th-system の走行部に載って運ばれるが、**th-system とは独立したシステム**である（§4 `HD-1`）。

| 担当 | 範囲 |
| --- | --- |
| **本システム** | 昇降機による**高さ**、カメラの**ヨー・ピッチ**、映像の表示、それらの操作画面 |
| th-system | 保管場所⇔試験場の移動、盤前への移動・停止、盤前での位置の微調整、機体の非常停止 |

th-system 側でも、カメラ昇降のピッチ・ヨー・高さは範囲外（別担当）とされている
（th-system [Spec.md](../../../../th-system/docs/plan/spec/Spec.md) §1・[Spec-onsite.md](../../../../th-system/docs/plan/spec/Spec-onsite.md) §7.1）。

**撮影した画像の保存・一覧までを含むかは未定**（[Spec-open.md](Spec-open.md) `H-S2`）。

---

## 2. 用語

th-system [Spec.md](../../../../th-system/docs/plan/spec/Spec.md) §2 の用語（機体・試験員・試験場・盤 など）をそのまま使う。
ここには本システムで新たに要る語だけを足す。

| 用語 | 意味 |
| --- | --- |
| 昇降機 | 機体に載り、カメラ部を上下させる機構 |
| カメラ部 | 昇降機の上端に付く無線カメラのユニット。ヨー・ピッチを動かせる |
| 操作者 | 本システムを操作する人。th-system の試験員と同じ人かは問わない |
| 移動システム | th-system のこと |

---

## 3. 目標

**未定**（[Spec-open.md](Spec-open.md) `H-G1`）。

th-system 側の目標で、本システムが関わるもの（出典: th-system [Spec.md](../../../../th-system/docs/plan/spec/Spec.md) §3）:

| th-system の目標 | 本システムとの関係 |
| --- | --- |
| **G1** 人・物への接触 0 件、意図しない挙動 0 件 | 本システムにも同じ水準を課すかは未定（`H-G1`）。昇降機が載ることで重心が高くなる予想が th-system 側にある（th-system [Spec-safety.md](../../../../th-system/docs/plan/spec/Spec-safety.md) §3） |
| **G2** カメラ昇降が要求する許容差内に、手動微調整なしで停止できる | **許容差を決めるのは本システム**（`H-V2`）。ヨー・ピッチで向きを直せるぶん緩められる見込み |

---

## 4. 設計思想

記号は `HD-n`（th-system の `SD-n` と区別する）。

* **HD-1 th-system から独立させる。**互いの仕様変更が波及しないようにする。**th-system とは通信しない**
  （どうしても要るときも最低限にし、先にここを更新する）。走行禁止などの**連動も設けない**。
  共有するのは機体（載せてもらう）と無線 AP だけ
* **HD-2 操作画面は th-system と別に持つ**
* **HD-3 高さは人が映像を見て決める。**盤ごとの高さを事前に登録・計測する仕組みを前提にしない（§6）

**HD-1 の帰結として受け入れていること:**

| 帰結 | 扱い |
| --- | --- |
| th-system の**画面の**非常停止では昇降機は止まらない | 受け入れる。**物理非常停止ボタンは電源系統を共有しているので昇降機も止まる**（信号のやり取りではなく電源の共有。[Spec-safety.md](Spec-safety.md) §4） |
| 昇降機が上がったまま機体が走れる | 既知のリスクとして残す（[Spec-open.md](Spec-open.md) §1） |
| AP を共有するので、映像が th-system の無線を圧迫しうる | 「干渉しない」を守るため映像の上限を決めて確かめる（`H-A8`） |

---

## 5. 全体構成

| 構成要素 | 中身 | 役割 |
| --- | --- | --- |
| **昇降部** | ESP32 ＋ モータドライバ Cytron MD10C ＋ 昇降機のモータ（**パワーウィンドウモータ**） ＋ **下端リミットスイッチ** ＋ **上向き超音波距離計**（昇降の高さ） | カメラ部を上下させる |
| **カメラ部**（無線ユニット） | ラズパイ 4 ＋ Web カメラ ＋ ヨー用ステッピングモータ ＋ ピッチ用サーボモータ ＋ モバイルバッテリ ＋ **上向き超音波距離計**（天井まで） | 映像を送り、向きを変える |
| 無線 | `th-rpi-ap`（th-system のラズパイが出す AP。**将来名前が変わる**） | 操作端末・昇降部・カメラ部をつなぐ |
| 操作端末 | 未定（`H-A9`） | カメラ部のラズパイが出す操作画面を開く |

**操作画面はカメラ部のラズパイが 1 画面で出す**（[Spec-ui.md](Spec-ui.md) §0）。
**昇降部のモータは th-system の駆動系と同じ電源系統**で、th-system の物理非常停止ボタンで一緒に電源が切れる。
AP の名前は将来変わるので、**名前を変えても設定の書き換えだけで済むようにする**。

---

## 6. 操作の流れ（盤前）

1. th-system が盤前に機体を止める（本システムは関与しない）
2. 操作者が本システムの画面を開き、**カメラ映像を見ながら昇降機を上昇させる**（速度はスライダーで変える。[Spec-ui.md](Spec-ui.md)）。上端・天井に近づいたら上昇は止まる（[Spec-safety.md](Spec-safety.md)）
3. **操作者がよいと思ったところで止める**
4. 必要ならヨー・ピッチで向きを合わせ、デジタルズームで拡大して盤上部を確認する（[Spec-ui.md](Spec-ui.md) §1.5）
5. 下降させてから、th-system 側で次の盤へ移動する

操作を補う工夫（映像の遅延を踏まえた速度の切替・ガイド表示など）の案は
[operation-ideas.md](../operation-ideas.md)（検討メモ。spec ではない）。

---

## 7. 目次

| 章 | ファイル | 状態 |
| --- | --- | --- |
| 未確定事項・決定の記録・th-system との関係 | [Spec-open.md](Spec-open.md) | 更新中 |
| 上下端と天井 | [Spec-safety.md](Spec-safety.md) | 初版 |
| 操作画面 | [Spec-ui.md](Spec-ui.md) | 初版（速度と設定画面） |
| 領域ごとの詳細 | [README.md](README.md) §2 の候補 | 未作成 |
