# 詳細設計書 — 昇降・カメラシステム

[完全設計書](../spec/Spec.md) を実装できる形に落とす。読み方は [README.md](README.md)。

> **2026-09-25 時点で骨格のみ。**機器構成は決まった（[Spec.md](../spec/Spec.md) §5）。
> 操作画面はカメラ部のラズパイが出すと決まった（[Spec-ui.md](../spec/Spec-ui.md) §0）。次は §2 を書く。

---

## 1. 設計方針

| ID | 方針 |
| --- | --- |
| **`DD-1`** | **名前を発明する余地をゼロにする。**必要な名前が無いと分かったら、実装する前に [-names.md](DetailedDesign-names.md) へ行を足す |
| **`DD-2`** | **判定ロジックはハードウェア・通信から切り離した純関数にする。**ホストで試験できるようにし、呼び出し側の配線は別の試験で縛る（th-system `follow_planner_core.py` と同じ二層構造） |
| **`DD-3`** | **裸の数値を書かない。**値には出どころ（実測・先方・逆算・暫定）を付ける |

## 2. 全体構成

未記入。書くこと: カメラ部のラズパイ（画面・映像・ヨー・ピッチ・天井の距離）と昇降部の ESP32（モータ・下端スイッチ・高さの距離）の間の通信、設定の保存場所と ESP32 への渡し方、天井の値が古いときに ESP32 側で上昇を止める仕組み（[Spec-safety.md](../spec/Spec-safety.md) §2 #4）。

## 3. 段階と作業パケット

未定。完全設計書の章ができたら、th-system と同じく**各段階の終わりに必ず「動くもの」が残る**ように切り、
`WP-<領域>-<番号>` で番号を振る。一覧と状態は [ImplementationPlan.md](../ImplementationPlan.md) §4 に置く。
